// Copy this file to AppSecrets.swift (gitignored) and fill in real values.
// appSharedSecret must match the backend's APP_SHARED_SECRET (backend/.env).
enum AppSecrets {
    static let stravaClientId = "YOUR_STRAVA_CLIENT_ID"
    static let appSharedSecret = "YOUR_APP_SHARED_SECRET"
}
